{#
You Are Here:
- Social Icons
- src/site/_includes/skeleventy/social-icons.njk

How It Works:
- On build, this is placed on every page with `{% include "social-icons.njk" %}`
- It is included in the footer by default
- SVGs come from the sprite sheet: images/icons/icon-library.svg

Dynamic Data:
- anything in {{ curly braces }} is a variable from
  (a) src/site/_data/social.json (for icons, links)
  (b) src/site/_data (for anything else, like theme color)
#}

<ul class="flex list-reset justify-center list-none" aria-label="Social Media Links">

  {% for item in social.networks %}
  {% if item.link != "" %}
  <li>
    <a href="{{ item.link | safe }}" class="inline-block lg:p-2 {% if loop.first %}-ml-2{% endif %} {% if item.icon != "" %}py-3{% else %}pl-2 pr-1 hover:text-{{ theme.colors.accent }}-500{% endif %} not-italic" aria-label="{{ item.name | capitalize }}" title="{{ item.name }}" target="_blank" rel="me noopener noreferrer">
    {% if item.icon != "" %}
      <svg class="block" role="img" aria-hidden="true" width="18" height="18">
        <use xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="/images/icons/icon-library.svg#{{ item.icon }}"></use>
      </svg>
    </a>
    {% else %}
      {{ item.name }}
    </a>{%- if not loop.last %}/{%- endif %}
    {% endif %}
  </li>
  {% endif %}
  {% endfor %}

</ul>
