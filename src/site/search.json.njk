---
permalink: search.json
---
{"search" : [
{%- for item in collections.archive %}
{%- if item.data.sitemapIgnore !== true %}
  {
    "url" : "{{ item.url }}",
    "title" : "{{ item.data.title }}",
    "text" : "{{ (item.data.title + " " + item.templateContent) | squash }}"
  }{% if not loop.last %},{% else %}{%- endif -%}
{%- endif %}
{%- endfor %}
]}
