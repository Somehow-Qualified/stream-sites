---
layout: default.njk
---
{#
You Are Here:
- Contact
- src/site/_layouts/contact.njk

How It Works:
- On build, this page adds a form (if enabled)

Dynamic Data:
- anything in {{ curly braces }} is a variable from:
  (a) frontmatter in the associated markdown file (src/site/page/contact.md)
  (b) src/site/_globals
#}
<section class="container max-w-5xl mx-auto mb-16">

  <h1 class="page-title text-5xl">{{ theme.contact.heading }}</h1>

  <div class="content highlight-{{ theme.colors.accent }}">

    {{ content | safe }}

    {% if form %}
      {% include "form.njk" %}
    {% endif %}

  </div>

</section>
